import React, { ReactElement } from 'react';

export default function ():ReactElement {
    return (
        <div className='flex items-center justify-center' style={{height: '90vh', width: '100vw', fontSize: '50px'}}>
            404 | This corner is empty
        </div>
    )
}